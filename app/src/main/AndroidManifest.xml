<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.slawomirmakurat.agromarket">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
                <!--<meta-data-->
                    <!--android:name="android.app.searchable"-->
                    <!--android:resource="@xml/searchable"></meta-data>-->
            </intent-filter>


        </activity>
        <activity android:name=".drawer.DrawerLav" />
        <activity android:name=".drawer.DrawerNavig" />
        <activity android:name=".drawer.DrawerAddFrend" />
        <activity android:name=".drawer.DrawerMessage" />
        <activity android:name=".drawer.DrawerMyAds" />
        <activity android:name=".drawer.DrawerMyFavorite" />
        <activity android:name=".drawer.DrawerAddAd" />
        <activity android:name=".drawer.DrawerSettings" />
        <activity android:name=".User.LoginActivity" />
        <activity android:name=".User.RegisterActivity"></activity>
    </application>

</manifest>